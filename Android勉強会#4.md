# Android 勉強会 #4

## デバイス（本体）の機能を使いたい場合について
Android端末（システム）の機能を使いたい場合は特別な権限を使うことをユーザーに認識させて、許諾を貰わなければいけない。
→個人情報に関わるものを取得できるので、そういったものをユーザーの許可なしで取得できないようにするため

ストアに申請する際にも、取得する情報や権限の種類などをあわせて申請しないといけない。
→記載が不十分だったり、実装がダメな場合、取得するための理由の明記が不十分だったりするとリジェクト（アプリのアップデート不可）をくらう
→書面に記載したり、アプリストアにも載るので丁寧に


年々セキュリティに対しては厳しくなっており、OSのアップデート毎に変更がないか特にチェックを念入りにしないといけない。
（OSのバージョンの差で実装・挙動が違ったりする場合も多い）
※今回は最新のOSバージョン（15）を基準にする

ドキュメント：
https://developer.android.com/guide/topics/permissions/overview?hl=ja

標準の権限と実行時の権限、OSに組み込むような特別な権限があるが
今回はここで言う実行時の権限を対象にする

標準の権限はインターネットを使うか等のよくある機能（なので危険性はないが、記載は必要）
OSに組み込むような特別な権限はプラットフォーマーがカスタマイズOSに組み込むもの。


権限一覧
https://developer.android.com/reference/android/Manifest.permission

かなり多いが、ニッチな機能以外はほぼ使わない
使う場合はAPIバージョンを確認するために使う



普通の設定（端末・アプリの設定）からでも後追いで変更できる
（Push通知が鬱陶しい場合はOFFにするなどはしたことある人がいるかも？）

## アプリでよく使う代表的な権限
・Push通知（NotificationPush)
・GPS（位置情報）
・カメラ
・ファイルへのアクセス

今回はPush通知、GPS、カメラを主軸に説明

## アプリから権限を要求する
https://developer.android.com/training/permissions/requesting?hl=ja

### AndroidManifest.xmlに使いたい権限を記載する
<uses-permission android:name="「権限の種類」" />

上記の申請にも関わるので必要最小限なものを選んで記載する。

### 特定の機能を使う「前」にユーザーに対して利用権限を確認する
権限確認せずに、使おうとするとアプリがクラッシュする。
アプリ申請時に利用の説明や、実装が不十分だったらリジェクト（アプリ申請を却下）されることもある
→経験上はリジェクトの理由としてアプリの権限周りの実装がうまく行ってない、正しく実装できない、不足しているなどはかなりありがち

※複数の権限を一度に許諾確認させることもできる

## Androidの権限に関する仕様とか注意点

### アプリの権限許諾(方針)の種類
アプリ使用時のみ許可と今回のみ許可、許可しないの3択がある。

・アプリ使用時のみ許可はアプリが起動中のみ
一般的に半永続的その権限の機能を使える状態にする。
ユーザが設定から後追いで変更する・再インストール・リセットしない限り持続される。

・今回のみ
アプリ起動して終了するまでその権限が有効化される
終了して再度アプリを起動する場合、また権限確認が行われる

・許可しない
その機能の利用を許可しなくなる
ただ、アプリの根幹的な機能である可能性もある
Push通知など不要なら切っておくなどはありがちかも


### アプリ権限の許諾を確認する

registerForActivityResult(ActivityResultContracts.RequestPermission())を使ってOSへ権限取得のダイアログを上げるように実装する
(Activityの遷移＆Result値を取得する実装と同じ。OSからの返り値を貰う実装)

許可、拒否に応じて追加実装する

### アプリの権限が拒否された場合
権限が拒否された場合、特定の機能は使えない
Androidのシステム上の仕様で拒否された後は「1度しか再度権限のリクエストが行えない」（2回しか行えない）
その後は権限ダイアログは表示されず、自動で拒否状態になる。


### 複数の権限の許諾（許諾されているか）を確認する
registerForActivityResult(ActivityResultContracts.RequestPermission())でtrueが返ってくる
もしくは
ContextCompat.checkSelfPermission(requireContext(), Manifest.permission.[権限]) == PackageManager.PERMISSION_DENIED 
で権限の状態だけ取得する
※　==　は下記で判定を分ける
PackageManager.PERMISSION_DENIED →　拒否状態 
PackageManager.PERMISSION_GRANTED →　許可状態